<style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            color: #333;
        }

        .container {
            width: 80%;
            margin: auto;
            overflow: hidden;
        }

        header {
            background: #505050;
            color: white;
            padding-top: 30px;
            min-height: 70px;
            border-bottom: #bbb 1px solid;
        }

        header a {
            color: #fff;
            text-decoration: none;
            text-transform: uppercase;
            font-size: 16px;
        }

        header ul {
            padding: 0;
            margin: 0;
            list-style: none;
            overflow: hidden;
        }

        header li {
            float: left;
            display: inline;
            padding: 0 20px 0 20px;
        }

        header #branding {
            float: left;
        }

        header #branding h1 {
            margin: 0;
        }

        header nav {
            float: right;
            margin-top: 10px;
        }

        header .highlight, header .current a {
            color: #e8491d;
            font-weight: bold;
        }

        header a:hover {
            color: #bbb;
            font-weight: bold;
        }

        

        .button_1 {
            height: 38px;
            background: #e8491d;
            border: 0;
            padding-left: 20px;
            padding-right: 20px;
            color: #fff;
        }

        

        article#main-col {
            float: left;
            width: 65%;
        }

        aside#sidebar {
            float: right;
            width: 30%;
            margin-top: 10px;
        }

        aside#sidebar .quote {
            padding: 20px;
            margin-bottom: 10px;
        }

        footer {
            padding: 20px;
            margin-top: 20px;
            color: #fff;
            background-color: #e8491d;
            text-align: center;
        }

        /* Стили для слайд-шоу */
        .slideshow-container {
            position: relative;
            
            height: 400px;
        }

        .mySlides {
            display: none;
            padding: 80px;
            text-align: center;
        }

        .prev, .next {
            cursor: pointer;
            position: absolute;
            top: 50%;
            width: auto;
            margin-top: -30px;
            padding: 16px;
            color: #888;
            font-weight: bold;
            font-size: 20px;
            border-radius: 0 3px 3px 0;
            user-select: none;
        }

        .next {
            position: absolute;
            right: 0;
            border-radius: 3px 0 0 3px;
        }

        .prev:hover, .next:hover {
            background-color: rgba(0, 0, 0, 0.8);
            color: white;
        }

        
        /* Стили для программы по дням */
        .itinerary-day {
            margin-bottom: 20px;
            padding: 15px;
            border: 1px solid #ddd;
            background-color: #fff;
        }

        .itinerary-day h3 {
            margin-top: 0;
            color: #e8491d;
        }

        /* Стили для дат и цен */
        .dates-prices {
            margin-top: 20px;
            padding: 15px;
            border: 1px solid #ddd;
            background-color: #fff;
        }

        .dates-prices table {
            width: 100%;
            border-collapse: collapse;
        }

        .dates-prices th, .dates-prices td {
            padding: 8px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        /* Стили для организационной информации */
        .org-info {
            margin-top: 20px;
            padding: 15px;
            border: 1px solid #ddd;
            background-color: #fff;
        }

        .tour-container {
            width: 80%;
            margin: auto;
            overflow: hidden;
            padding: 20px 0;
        }

        /* Filter form styles */
        .filter-form {
            background-color: #fff;
            padding: 20px;
            margin-bottom: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .filter-form h2 {
            margin-top: 0;
            color: #e8491d;
            font-size: 1.5em;
        }
        .filter-form label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }
        .filter-form input[type="date"],
        .filter-form select,
        .filter-form input[type="number"],
        .filter-form input[type="text"],
        .filter-form input[type="email"] {
            width: 100%;
            padding: 8px;
            margin-bottom: 10px;
            border: 1px solid #ddd;
            border-radius: 4px;
            box-sizing: border-box; /* Ensures padding and border are included in the element's total width and height */
        }
        .filter-form select {
            appearance: none; /* Remove default arrow in some browsers */
            background-image: url('data:image/svg+xml;charset=UTF-8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="%23333"><path d="M7.41 8.59L12 13.17l4.59-4.58L18 10l-6 6-6-6 1.41-1.41z"/></svg>');
            background-repeat: no-repeat;
            background-position: right 8px top 50%, 0 0;
            background-size: 15px;
            padding-right: 30px;
        }
        .filter-form button {
            background-color: #e8491d;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 1em;
        }
        .filter-form button:hover {
            background-color: #c7370f;
        }

        /* Tour grid styles */
        .tour-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
        }

        /* Tour block styles */
        .tour-block {
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 15px;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.05);
        }
        .tour-block img {
            width: 100%;
            height: auto;
            border-radius: 8px;
            margin-bottom: 10px;
            object-fit: cover; /* Ensure images fill the container without distortion */
            height: 200px; /* Standard height for all images */
        }
        .tour-block h3 {
            color: #e8491d;
            margin-top: 0;
            margin-bottom: 5px;
            
        }
        .tour-block a {
            color: #e8491d;
            margin-top: 0;
            margin-bottom: 5px;
            
        }
        .tour-block p {
            margin-bottom: 8px;
        }
        .tour-block .duration {
            font-style: italic;
            color: #666;
        }

        /* Responsive layout */
        @media (min-width: 768px) {
            .tour-container {
                width: 90%;
            }
        }
        @media (min-width: 992px) {
            .tour-container {
                width: 80%;
            }
        }
        .flash-messages {
                    margin-top: 20px;
                    padding: 10px;
                    border: 1px solid;
                    margin-bottom: 15px;
                    border-radius: 4px;
                }
                .flash-success {
                    color: #155724;
                    background-color: #d4edda;
                    border-color: #c3e6cb;
                }
                .flash-warning {
                    color: #856404;
                    background-color: #fff3cd;
                    border-color: #ffeeba;
                }
                .flash-danger {
                    color: #721c24;
                    background-color: #f8d7da;
                    border-color: #f5c6cb;
                }
                .flash-info {
                    color: #0c5460;
                    background-color: #d1ecf1;
                    border-color: #bee5eb;
                }
                #showcase {
            min-height: 400px;
            background: url('/images/main-bg.jpg') no-repeat 0 -400px;
            background-size: cover;
            text-align: center;
            color: #fff;
            position: relative;
        }

        #showcase:before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
        }

        #showcase h1 {
            margin-top: 100px;
            font-size: 55px;
            margin-bottom: 10px;
            position: relative;
            z-index: 1;
        }

        #showcase p {
            font-size: 20px;
            position: relative;
            z-index: 1;
        }

    
        h2 {
            margin-top: 0;
            color:rgb(255, 83, 36);
        }

        

        .boxes {
            margin-top: 20px;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
        }

        .box {
            text-align: center;
            padding: 20px;
            border: 1px solid #ddd;
            background: #fff;
            border-radius: 5px;
            transition: transform 0.3s;
        }

        .box:hover {
            transform: translateY(-5px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .box img {
            width: 100px;
            height: 100px;
            object-fit: cover;
            
            margin-bottom: 15px;
        }

        .box h3 {
            color: #e8491d;
        }

        .popular-tours {
            padding: 40px 0;
            background: #fff;
        }

        .popular-tours h2 {
            text-align: center;
            color: #333;
            margin-bottom: 30px;
        }

        .tour-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
        }

        .tour-block {
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 15px;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.05);
            transition: transform 0.3s;
        }

        .tour-block:hover {
            transform: translateY(-5px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .tour-block img {
            width: 100%;
            height: 200px;
            border-radius: 8px;
            margin-bottom: 10px;
            object-fit: cover;
        }

        .tour-block h3 {
            color: #e8491d;
            margin-top: 0;
            margin-bottom: 5px;
        }

        .tour-block a {
            color: #e8491d;
            text-decoration: none;
        }

        .tour-block .duration {
            font-style: italic;
            color: #666;
        }

        .tour-block .price {
            font-weight: bold;
            color: #333;
            margin-top: 10px;
        }

        .testimonials {
            background: #f9f9f9;
            padding: 40px 0;
            text-align: center;
        }

        .testimonials h2 {
            margin-bottom: 30px;
        }

        .testimonial {
            background: #fff;
            padding: 20px;
            margin: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            margin: 0 auto 20px;
        }

        .testimonial img {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            object-fit: cover;
            margin-bottom: 15px;
        }

        .testimonial p {
            font-style: italic;
        }

        .testimonial .author {
            font-weight: bold;
            margin-top: 10px;
        }




        #main {
            padding: 40px 0;
            background: #fff;
        }

        #main h1 {
            font-size: 2.5em;
            margin-bottom: 20px;
            color: #e8491d;
            text-align: center;
        }

        #main h2 {
            color: #e8491d;
            margin-top: 30px;
        }

        .about-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin-top: 30px;
        }

        .about-text {
            padding-right: 20px;
        }

        .about-image img {
            width: 100%;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .mission-vision {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin: 40px 0;
        }

        .mission, .vision {
            background: #f9f9f9;
            padding: 25px;
            border-radius: 10px;
            border-left: 4px solid #e8491d;
        }

        

        .stats {
            background: #505050;
            color: #fff;
            padding: 50px 0;
            text-align: center;
        }

        .stats-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 30px;
        }

        .stat-item {
            padding: 20px;
        }

        .stat-item .number {
            font-size: 3em;
            font-weight: bold;
            color: #e8491d;
            margin-bottom: 10px;
        }

        .cta {
            background:#505050;
            color: #fff;
            text-align: center;
            padding: 50px 0;
        }

        .cta h2 {
            color: #fff;
            margin-bottom: 20px;
        }
    </style>
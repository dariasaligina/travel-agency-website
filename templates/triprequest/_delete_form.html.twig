<form method="post" action="{{ path('app_triprequest_delete', {'id': triprequest.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ triprequest.id) }}">
    <button class="btn">Удалить</button>
</form>
